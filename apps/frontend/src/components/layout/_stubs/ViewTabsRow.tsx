'use client';

// Compat re-export: canonical implementation lives in `../PageHeader.tsx`.
// Kept here so any previously-written page imports continue to resolve.
export { ViewTabsRow as default } from '../PageHeader';
